services:
  bastos-edi:
    image: abasty/bastos-edi
    container_name: bastos-edi
    network_mode: "host"
    user: "${USER_ID:-1000}:${GROUP_ID:-1000}"
    volumes:
      - /etc/passwd:/etc/passwd:ro
      - /etc/group:/etc/group:ro
      - ./disk:/app/disk
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
